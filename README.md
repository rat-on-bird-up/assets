# assets

Public asset host for things that need a stable external URL – above all
Notion page covers, which accept only public HTTPS links and hotlink them
forever. Anything here is public by definition; never commit personal data.

Layout: `notion/<database>/<image>.jpg`, referenced as
`https://raw.githubusercontent.com/rat-on-bird-up/assets/main/<path>`.
